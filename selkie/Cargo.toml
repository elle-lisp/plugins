[package]
name = "elle-selkie"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
elle-plugin = { git = "https://github.com/elle-lisp/elle", branch = "w2" }
selkie-rs = { version = "0.3", default-features = false }
