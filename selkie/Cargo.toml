[package]
name = "elle-selkie"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
elle-plugin.workspace = true
selkie-rs = { version = "0.3", default-features = false }
