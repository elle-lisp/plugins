[package]
name = "elle-polars"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
elle-plugin = { git = "https://github.com/elle-lisp/elle", branch = "main" }
polars = { version = "0.46", features = ["lazy", "csv", "parquet", "json", "dtype-struct"] }
